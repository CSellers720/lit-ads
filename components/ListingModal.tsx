import Modal from 'react-bootstrap/Modal';

interface ModalProps {
    showModal: boolean;
    modalPhase: number;
}

function ListingModal(modalProps: ModalProps): JSX.Element {
    return (
        
    );
};

export default ListingModal;
